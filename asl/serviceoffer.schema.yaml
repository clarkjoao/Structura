openapi: 3.1.0
info:
  title: ServiceOffer
  version: v1
  description: "Oferta de serviço"

components:
  schemas:
    ServiceOfferSpec:
      type: object
      required:
        - description
      properties:
        description:
          type: string
        displayName:
          type: string
          description: Friendly display name for the service offer
