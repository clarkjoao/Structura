openapi: 3.1.0
info:
  title: Squad
  version: v1
  description: Time multidisciplinar

components:
  schemas:
    SquadSpec:
      type: object
      required:
        - code
      properties:
        code:
          type: string
          description: Squad identifier/code
        displayName:
          type: string
          description: Friendly display name for the squad
