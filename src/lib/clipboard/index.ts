/**
 * Clipboard: read/write draw.io XML and SVG via the browser clipboard API.
 */
export {
  writeDrawioToClipboard,
  readDrawioFromClipboard,
  readStructuraClipboard,
  readSvgFromClipboard,
} from "./clipboard";
