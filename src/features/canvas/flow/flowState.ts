/** @deprecated Use `@/features/flows` imports instead. */
export type { FlowHighlight, RecordingInfo, CoverageInfo } from "@/features/flows";
export {
  EMPTY_FLOW_HIGHLIGHT,
  buildFlowHighlight,
  buildCoverage,
  buildRecordingInfo,
} from "@/features/flows";
