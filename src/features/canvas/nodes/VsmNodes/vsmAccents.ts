/**
 * Default accents that differ from the flow family's slate. Inventory and the
 * kaizen burst are amber by VSM convention — the colour of something to act on.
 * Theme tokens, like every preset, so they follow dark mode.
 */
export const VSM_AMBER = "hsl(var(--node-person))";
