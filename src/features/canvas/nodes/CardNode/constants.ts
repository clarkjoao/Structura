/** Painted card width bounds (CSS `min-w` / `max-w` on CardNode). */
export const CARD_MIN_W = 200;
export const CARD_MAX_W = 260;

/** Dim non-participating cards while a flow is being recorded. */
export const CARD_RECORDING_DIM_OPACITY = 0.35;
