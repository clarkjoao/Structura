/**
 * Feature flags for the QuickActions toolbar feature.
 * When ENABLE_LEGACY_PANEL_ACTIONS is false (default), migrated controls
 * are hidden from side panels and only the QuickActions toolbar is shown.
 */
export const ENABLE_LEGACY_PANEL_ACTIONS = false;
