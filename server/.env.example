# ── Server ───────────────────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
WS_PATH=/ws

# ── HTTPS (optional — omit to use plain HTTP behind a reverse proxy) ──────────
# SSL_KEY_PATH=/etc/ssl/private/server.key
# SSL_CERT_PATH=/etc/ssl/certs/server.crt
